[tox]
requires = tox>=4
envlist = py313, py314, lint

[testenv]
extras = dev
commands = pytest {posargs}

[testenv:lint]
skip_install = True
deps = ruff>=0.9
commands =
    ruff check kivy_ios/ tests/
    ruff format --check kivy_ios/ tests/
